# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
# org.gradle.java.home is intentionally not set, using system JAVA_HOME

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21.1
loader_version=0.18.4
loom_version=1.15-SNAPSHOT

# Mod Properties
mod_version=1.4.6.2
maven_group=damage.engine
archives_base_name=damage-engine

# ModMenu (compile only, optional at runtime)
modmenu_version=11.0.3

# Fabric API (dev dependency, resolves at runtime via the "fabric-api" depends entry)
fabric_api_version=0.116.15+1.21.1
