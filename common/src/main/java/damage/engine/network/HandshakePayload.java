package damage.engine.network;

/**
 * C2S handshake payload, used by the client to detect whether the server has Damage Engine installed.
 * Pure data record (platform-independent).
 */
public record HandshakePayload() {
}
